<Window x:Class="GmEcuSimulator.Views.DbcImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导入 DBC"
        Height="620" Width="560"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FontSize="14"
        Style="{StaticResource Window.Dialog}">

    <DockPanel LastChildFill="True">

        <!-- Custom titlebar -->
        <Grid DockPanel.Dock="Top" Background="{DynamicResource Chrome.GradientBrush}" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="导入 DBC"
                       Style="{StaticResource Text.Body}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"/>
            <Button Grid.Column="1"
                    Style="{StaticResource Button.ChromeClose}"
                    Width="46" Height="32"
                    Click="OnCancelClicked"
                    ToolTip="取消">
                <Path Data="{StaticResource Icon.Close}"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.2"
                      Width="10" Height="10"/>
            </Button>
        </Grid>

        <!-- Footer. Fresh ECU: OK imports the picked messages. ECU with existing broadcasts: OK
             reconciles - de-ticked existing ids are removed, newly ticked ids appended, rows not in
             this DBC left untouched. -->
        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource Chrome.BgBrush}"
                BorderBrush="{DynamicResource Border.SubtleBrush}"
                BorderThickness="0,1,0,0"
                Padding="14,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" IsCancel="True" MinWidth="90" Click="OnCancelClicked"/>
                <Button Content="确定"
                        IsDefault="True" MinWidth="110" Margin="10,0,0,0"
                        Click="OnImportClicked"/>
            </StackPanel>
        </Border>

        <!-- Transmitter picker + select all/none -->
        <Border DockPanel.Dock="Top" Padding="14,12,14,8">
            <StackPanel>
                <TextBlock Text="{Binding FileName}" Style="{StaticResource Text.Subtitle}"/>
                <TextBlock Text="DBC 描述整个总线。选择发送模块，然后选择要广播的消息。"
                           Style="{StaticResource Text.Caption}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                <TextBlock Text="This ECU already has broadcasts. Ticked rows below are its existing CAN ids found in this DBC - untick one to remove it, tick a new one to add it. Rows whose id isn't in this DBC are left as they are."
                           Style="{StaticResource Text.Caption}"
                           Foreground="{DynamicResource Accent.PrimaryBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,8"
                           Visibility="{Binding ReconcileMode, Converter={StaticResource BoolToVis}}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="发送器" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding Transmitters}"
                              DisplayMemberPath="Display"
                              SelectedItem="{Binding SelectedTransmitter}"
                              Height="34"/>
                    <Button Grid.Column="2" Content="全选" Margin="8,0,0,0" Padding="10,4"
                            Command="{Binding SelectAllCommand}"/>
                    <Button Grid.Column="3" Content="全不选" Margin="6,0,0,0" Padding="10,4"
                            Command="{Binding SelectNoneCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Message checklist -->
        <Border Margin="14,0,14,12" CornerRadius="4"
                BorderBrush="{DynamicResource Border.SubtleBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Messages}" Margin="8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Selected}" Margin="0,3">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Display}" FontFamily="{StaticResource Font.Mono}"/>
                                    <TextBlock Text="{Binding Hint}"
                                               Foreground="{DynamicResource Accent.PrimaryBrush}"
                                               Margin="10,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </DockPanel>
</Window>
